{
	"devDependencies": {
		"typescript": "6.0.2",
		"@types/bun": "1.3.12"
	},
	"scripts": {
		"setup": "bun install --frozen-lockfile",
		"setup-production": "bun install --frozen-lockfile --production true",
		"typecheck": "bun --bun tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {}
}
